package repository

func FindSensors() error {
	//scan, cursor, err := application.RedisClient.Scan(0, "sensor:*", 1).Result()
	//if err != nil {
	//	return errors.Wrap(err, "Can't scan sensors")
	//}
	return nil
}
